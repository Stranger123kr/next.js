/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.pixabay.com",
      "via.placeholder.com",
      "images.unsplash.com",
      "cdn1.iconfinder.com",
    ],
  },
};

export default nextConfig;
